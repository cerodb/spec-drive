---
spec: "{{spec_name}}"
phase: design
created: "{{timestamp}}"
---

# Design: {{spec_name}}

## Architecture Overview

<!-- High-level structure: components, their relationships, and how data flows between them. A diagram (Mermaid or ASCII) is strongly recommended. -->

## Components

<!-- For each component: responsibility, inputs, outputs, dependencies. Reference specific acceptance criteria (AC-N.N) that each component satisfies. -->

## Data Flow

<!-- How information moves through the system end-to-end. Include the happy path and key error paths. -->

## Technical Decisions

<!-- Each decision: what was chosen, what alternatives were considered, why this option wins. Decisions must trace to constraints from idea.md or requirements from requirements.md. -->

## Error Handling

<!-- How the system fails gracefully: error categories, recovery strategies, user-facing error messages. What happens when external dependencies are unavailable. -->
