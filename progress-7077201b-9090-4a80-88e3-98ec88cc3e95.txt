# Progress Log — Run 7077201b-9090-4a80-88e3-98ec88cc3e95

## Completed Stories

### US1: Fix readlink -f GNU portability in hook scripts
- Status: DONE
- Commit: 74fd81d
- Changes:
  - hooks/scripts/resolve-config.sh: Added portable_realpath() helper using python3 os.path.realpath with cd/pwd -P fallback
  - hooks/scripts/stop-watcher.sh: Replaced readlink -f calls (lines 33, 40) with portable_realpath()
  - hooks/scripts/context-loader.sh: Replaced readlink -f calls (lines 29, 36) with portable_realpath()
  - test/test-hooks.sh: Added 4 new tests for portable_realpath portability and absence of bare readlink -f
- Tests: npm test exits 0 (140 total tests, 0 failures)

### US2: Fix find -mmin GNU portability in stop-watcher.sh
- Status: DONE
- Commit: 6fee157
- Changes:
  - hooks/scripts/stop-watcher.sh: Replaced `find ... -mmin +60 -delete` (line 241) with portable `_cleanup_old_progress_files()` helper
  - _cleanup_old_progress_files priority: python3 os.path.getmtime → stat -c %Y (Linux) → stat -f %m (macOS) → skip gracefully
  - test/test-hooks.sh: Added 4 new tests: AC1 (no find -mmin in code), AC4 (guarded by is_safe_spec_path), AC2 (deletes old files), AC3 (preserves recent files)
- Tests: npm test exits 0 (144 total tests, 0 failures)

## Codebase Patterns

### Shell Portability
- resolve-config.sh is the shared library sourced by all hook scripts (stop-watcher.sh and context-loader.sh source it with `. "$SCRIPT_DIR/resolve-config.sh"`)
- portable_realpath() priority: python3 → realpath → cd+pwd -P for dirs → dirname+cd+pwd -P for files
- Portable mtime check pattern: python3 os.path.getmtime (first) → stat -c %Y (Linux) → stat -f %m (macOS) → skip gracefully
- Avoid GNU-only find flags: -mmin, -printf, -exec {} \+ (not portable to macOS BSD find)
- All hooks use set -euo pipefail — prefer `|| true` guards on fallible non-critical paths
- Hooks bail early with `exit 0` (not `exit 1`) on non-fatal conditions to avoid breaking Claude sessions

### Testing
- test-hooks.sh uses bash `source .` to test functions from resolve-config.sh inline
- mktemp -d for tmpdir creation; EXIT trap for cleanup
- Ambiguity safety tests create multiple state files and verify hook output text

### Architecture
- Hook scripts are bash-only, no compilation
- resolve-config.sh: config resolution utilities + portable helpers
- stop-watcher.sh: Stop hook (drives execution loop continuation)
- context-loader.sh: SessionStart hook (outputs active project context to stderr)

### US3: Add smoke integration test for minimal project creation flow
- Status: DONE
- Commit: 0d6ca12
- Changes:
  - test/test-smoke.sh: New smoke test (10 assertions) — creates tmpdir, writes .spec-drive-state.json with name/basePath/phase fields, writes idea.md with frontmatter, validates all required fields and artifacts, cleans up on EXIT trap
  - package.json: Added 'test:smoke': 'bash test/test-smoke.sh' script; added test:smoke to main 'test' chain
- Tests: npm test exits 0 (154 total tests, 0 failures — up from 144)

### US4: Add GitHub Actions CI with Linux and macOS matrix runners
- Status: DONE
- Commit: 327ac15
- Changes:
  - .github/workflows/ci.yml: New CI workflow with matrix strategy (ubuntu-latest, macos-latest)
  - Triggers on push and pull_request targeting main and feat/* branches
  - Installs jq via apt-get (Linux) or brew (macOS) before running tests
  - Uses actions/checkout@v4 and actions/setup-node@v4 (Node 20)
  - Runs 'npm test' as primary test step
- Tests: YAML validated with python3 yaml.safe_load; npm test exits 0 (154 tests, 0 failures)

### US5: Implement /spec-drive:list command prompt
- Status: DONE
- Commit: 6a7f0f6
- Changes:
  - commands/list.md: New command prompt for /spec-drive:list
    - Valid YAML frontmatter: description, argument-hint: "", allowed-tools: [Read, Bash, Glob]
    - Step 1: Resolves PROJECT_ROOT via same config-precedence chain as other commands
    - Step 2: Enumerates all .spec-drive-state.json under PROJECT_ROOT (find -maxdepth 2)
    - Step 3: Identifies active project via ~/.spec-drive-active.json, falls back to cwd scan
    - Step 4: Sorts projects by last-activity timestamp descending
    - Step 5: Displays aligned table with name/phase/taskIndex/totalTasks/last-activity
    - Portable mtime: stat -c %Y (Linux) / stat -f %m (macOS)
    - Active project marked with * prefix; empty-project and single-project edge cases handled
  - test/test-commands.sh: Added 'list' to COMMANDS array (line 21) so frontmatter validation covers new file
- Tests: npm test exits 0 (157 total tests, 0 failures — up from 154)

### US6: Register list command in plugin.json and add targeted tests
- Status: DONE
- Commit: da87437
- Changes:
  - .claude-plugin/plugin.json: Added "commands" array with all 10 commands registered (new, research, requirements, design, tasks-cmd, implement, status, cancel, list, help). Each entry has name, path, and description fields. JSON validates with python3 -m json.tool.
  - test/test-commands.sh: Added dedicated "list command completeness" test block (5 new assertions): description/argument-hint/allowed-tools frontmatter keys present, body references PROJECT_ROOT, body references phase output
- Tests: npm test exits 0 (163 total tests, 0 failures — up from 157)

## Codebase Patterns

### Plugin Registration
- .claude-plugin/plugin.json "commands" array: each entry needs name (string), path (relative to plugin root, e.g. "commands/list.md"), description (string)
- JSON format — validate with: python3 -m json.tool .claude-plugin/plugin.json
- All 10 commands registered: new, research, requirements, design, tasks-cmd, implement, status, cancel, list, help

### US7: Implement /spec-drive:switch command and document active-project registry
- Status: DONE
- Commit: 6b5b4b1
- Changes:
  - commands/switch.md: New switch command prompt with valid YAML frontmatter (description, argument-hint, allowed-tools: [Read, Bash, Glob])
    - Step 1: Resolves PROJECT_ROOT via same config-precedence chain as other commands
    - Step 2: Enumerates all .spec-drive-state.json under PROJECT_ROOT (find -maxdepth 2)
    - Step 3: Presents numbered selection menu with name/phase/tasks/last-activity
    - Step 4: Accepts selection by number or case-insensitive prefix name match
    - Step 5: Writes ~/.spec-drive-active.json as {activePath, name, switchedAt} using atomic mv pattern
    - Step 6: Confirms the switch with path/phase/timestamp output
    - Step 7: Documents compatibility fallback (no registry = cwd scan detection)
    - Active Registry Format Reference table: activePath/name/switchedAt fields with descriptions
  - schemas/spec-drive.schema.json: Added $defs.activeRegistry section documenting ~/.spec-drive-active.json format (activePath, name, switchedAt). Added $comment at top-level documenting registry path and fallback. JSON validates with jq.
  - test/test-commands.sh: Added 'switch' to COMMANDS array (now 11 commands). Added 6 targeted switch tests: description/argument-hint/allowed-tools frontmatter keys, ~/.spec-drive-active.json reference, activePath+switchedAt registry format, cwd fallback documentation.
  - .claude-plugin/plugin.json: Registered switch command with name/path/description.
- Tests: npm test exits 0 (173 total tests, 0 failures — up from 163)

## Codebase Patterns

### Switch / Active Registry
- ~/.spec-drive-active.json format: {activePath: string, name: string, switchedAt: ISO-8601-string}
- Atomic write pattern for registry: mktemp ~/.spec-drive-active.json.XXXXXX → write → mv (same pattern as research.md state updates)
- Selection accepts number (1-based) or name (exact/case-insensitive prefix); 'q' to cancel
- All commands follow cwd-scan fallback when registry absent — switch is always optional

### US8: Register switch command in plugin.json and add targeted tests
- Status: DONE
- Commit: (this commit)
- Changes:
  - All US8 acceptance criteria were already satisfied by US7 implementation.
  - .claude-plugin/plugin.json: switch already registered with name/path/description (done in US7 commit 6b5b4b1)
  - test/test-commands.sh: dedicated "switch command completeness" block already present with 6 assertions (done in US7)
  - Assertions cover: description/argument-hint/allowed-tools frontmatter keys, ~/.spec-drive-active.json reference, activePath+switchedAt registry format, cwd fallback documentation
  - npm test exits 0: 173 tests, 0 failures
- Tests: bash test/test-commands.sh exits 0; bash test/test-structure.sh exits 0; npm test exits 0 (173 tests)

### US9: Add requirementsSha and designSha staleness fields to state schema
- Status: DONE
- Commit: 332cb12
- Changes:
  - schemas/spec-drive.schema.json: Added two optional string properties before taskResults:
    - `requirementsSha`: "SHA of requirements.md at time tasks.md was generated"
    - `designSha`: "SHA of design.md at time tasks.md was generated"
    - Both are NOT in the required array (optional fields)
    - Existing required properties (name, basePath, phase) and enums unchanged
  - test/test-schema.sh: Added 11 new assertions (tests 6-8 blocks):
    - requirementsSha exists, is type string, description references requirements.md
    - designSha exists, is type string, description references design.md
    - Both fields absent from required array (verified optional)
    - Existing required props (name, basePath, phase) still present
- Tests: npm test exits 0 (187 total tests, 0 failures — up from 173; schema tests grew from 9 to 20)

## Codebase Patterns

### Schema Staleness / SHA Fields
- requirementsSha and designSha are optional string properties in .spec-drive-state.json
- Intended usage: set when tasks.md is generated from design.md/requirements.md; compare against current SHA to detect upstream changes
- SHA generation: `git hash-object <file>` or `sha256sum <file> | cut -c1-64`
- Both fields absent by default (optional); never add them to "required" array

### US10: Implement /spec-drive:refactor command prompt
- Status: DONE
- Commit: d25b7c9
- Changes:
  - commands/refactor.md: New command with valid YAML frontmatter (description, argument-hint: "", allowed-tools: [Read, Write, Edit, Bash, Glob, Grep])
    - Step 1: Finds active project via cwd scan / ~/.spec-drive-active.json fallback
    - Step 2: Reads .progress.md and extracts Learnings section as refactor trigger
    - Step 3: Reads state and validates phase is >= requirements (not idea/research)
    - Step 4: Detects staleness via requirementsSha/designSha comparison (git hash-object or sha256sum fallback)
    - Steps 5-7: Updates artifacts in sequential order: requirements.md -> design.md -> tasks.md
    - Step 8: Appends CHANGELOG entry to .progress.md (timestamp, trigger, per-artifact summary, rationale)
    - Step 9: Rewrites requirementsSha/designSha in state; resets phase to "tasks" if tasks.md changed during execution
    - Step 10: Confirms completion with next-steps hints; references phase-transitions.md for valid navigation
  - test/test-commands.sh: Added 'refactor' to COMMANDS array (now 12 commands). Added 8-assertion "refactor command completeness" block: description/argument-hint/allowed-tools frontmatter keys, .progress.md reference, requirements/design/tasks sequential order, requirementsSha+designSha staleness, CHANGELOG reference, phase-transitions.md reference
  - .claude-plugin/plugin.json: Registered refactor command with name/path/description (now 12 commands total)
- Tests: npm test exits 0 (207 total assertions, 0 failures — up from 187)

## Codebase Patterns

### Refactor / Spec Iteration
- Refactor is valid from any phase >= requirements (requirements, design, tasks, execution)
- .progress.md Learnings section is the canonical trigger for when to run /spec-drive:refactor
- Staleness detection: compare current sha256sum/git hash-object against requirementsSha/designSha stored in state
- Sequential update order MUST be requirements → design → tasks (downstream artifacts depend on upstream)
- If tasks.md changes while phase=execution, reset phase back to "tasks" so implement re-validates before continuing
- CHANGELOG entries in .progress.md: timestamp + per-artifact summary + rationale (not just what changed, but why)
- Atomic state write: mktemp same-dir temp → write → mv (consistent with research.md pattern)

### US11: Register refactor command in plugin.json and add targeted tests
- Status: DONE
- Commit: (see US10 commit d25b7c9 — all US11 AC were satisfied there)
- Changes:
  - All US11 acceptance criteria were already satisfied by US10 implementation.
  - .claude-plugin/plugin.json: refactor already registered with name/path/description (done in US10 commit d25b7c9)
  - test/test-commands.sh: dedicated "refactor command completeness" block already present with 8 assertions (done in US10):
    - description/argument-hint/allowed-tools frontmatter keys
    - .progress.md reference as source for execution learnings
    - Sequential update order: requirements → design → tasks
    - requirementsSha and designSha staleness detection references
    - CHANGELOG section reference in .progress.md
    - phase-transitions.md navigation reference
  - npm test exits 0: 196 total assertions, 0 failures (47 structure + 23 hooks + 70 commands + 20 schema + 26 cross-cli + 10 smoke)
- Tests: bash test/test-commands.sh exits 0; bash test/test-structure.sh exits 0; npm test exits 0 (196 tests)

### US12: Update phase-transitions.md to include the refactor path
- Status: DONE
- Commit: ba70257
- Changes:
  - skills/spec-workflow/references/phase-transitions.md: Added ## Refactor section documenting:
    - Valid entry states table: completed (post-completion learnings) and execution (mid-cycle discovery)
    - Requirement that any phase >= requirements is valid entry; research/idea rejected (no artifacts)
    - Update loop: requirements.md → design.md → tasks.md → (resume execution) with rationale for strict order
    - Staleness re-check trigger: requirementsSha/designSha comparison; SHA generation via git hash-object / sha256sum fallback
    - Phase reset rule: if entry was execution, phase resets to tasks after refactor so implement re-validates
  - test/test-structure.sh: Added 4 new assertions in "Checking phase-transitions.md Refactor section..." block:
    - AC1: `## Refactor` heading exists in phase-transitions.md
    - AC2: completed and execution entry states documented
    - AC3: requirements.md, design.md, tasks.md all referenced in update loop
    - AC4: requirementsSha and designSha staleness trigger documented
- Tests: npm test exits 0 (200 total assertions, 0 failures — 51 structure + 23 hooks + 70 commands + 20 schema + 26 cross-cli + 10 smoke)

### US13: Update README and INSTALL for v1.1 command surface and macOS support
- Status: DONE
- Commit: b77ad09
- Changes:
  - README.md: Added "## Commands" table with all 12 commands including list, switch, refactor. Added "## macOS Compatibility" section documenting portable_realpath() and portable mtime check, macOS prerequisites (jq via brew), and CI matrix note.
  - INSTALL.md: Added "## Available Commands (v1.1)" section with full 12-command table including list, switch, and refactor, plus usage examples for list/switch and refactor.
  - package.json: Bumped version from 1.0.2 → 1.1.0
  - .claude-plugin/plugin.json: Bumped version from 1.0.2 → 1.1.0
- Tests: npm test exits 0 (200 total assertions, 0 failures — 51 structure + 23 hooks + 70 commands + 20 schema + 26 cross-cli + 10 smoke)

### US14: Final release validation: full suite run and v1.1.0 go/no-go
- Status: DONE
- Commit: (this commit)
- Changes:
  - Full test suite run: 6 suites, 200 total assertions, 0 failures
    - test-structure.sh: 51 passed (includes refactor section in phase-transitions.md)
    - test-hooks.sh: 23 passed (includes portable_realpath and no find -mmin)
    - test-commands.sh: 70 passed (includes list/switch/refactor completeness)
    - test-schema.sh: 20 passed (includes requirementsSha/designSha optional fields)
    - test-cross-cli.sh: 26 passed
    - test-smoke.sh: 10 passed
  - AC1: npm test exits 0 ✓
  - AC2: No unguarded `readlink -f` in hooks/ — only a comment line ✓
  - AC3: No `-mmin` flags in hooks/ — only a comment line ✓
  - AC4: plugin.json version == package.json version == 1.1.0 ✓
  - AC5: README.md contains list, switch, refactor command names (lines 192-194) ✓
  - AC6: npm test exits 0 ✓
  - GO/NO-GO: **GO** — v1.1.0 ready to tag and publish
- Tests: npm test exits 0 (200 total assertions, 0 failures — 51 structure + 23 hooks + 70 commands + 20 schema + 26 cross-cli + 10 smoke)
