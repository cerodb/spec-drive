{
  "light": { "mechanism": "subprocess", "cmd": "coda-batch --model {MODEL} -pf {promptfile}" },
  "standard": { "mechanism": "subprocess", "cmd": "coda-batch --model {MODEL} -pf {promptfile}" },
  "advanced": { "mechanism": "subprocess", "cmd": "coda-batch --model {MODEL} -pf {promptfile}" },
  "frontier": { "mechanism": "subprocess", "cmd": "coda-batch --model {MODEL} -pf {promptfile}" }
}
