{
  "light": { "mechanism": "subprocess", "cmd": "codex exec -m gpt-5.6-luna -s workspace-write -- < {promptfile}" },
  "standard": { "mechanism": "subprocess", "cmd": "codex exec -m gpt-5.6-terra -s workspace-write -- < {promptfile}" },
  "advanced": { "mechanism": "subprocess", "cmd": "codex exec -m gpt-5.5 -s workspace-write -- < {promptfile}" },
  "frontier": { "mechanism": "subprocess", "cmd": "codex exec -m gpt-5.6-sol -s workspace-write -- < {promptfile}" }
}
